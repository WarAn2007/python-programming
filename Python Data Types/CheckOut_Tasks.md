# set:
1.Create mupltiple sets about fruits(national,exotic, tropical) and create united set called "Fruits". Pop out all from national and alocate in
'Non-local_fruits'

# dict:
1.Collect data about our family in nested dict(name,age,ocupation,family role), by update and durect input methods. 
Copy dict and replace (names, family role and age) by update and durect input methods, if more people need add, if less reduce with pop() and del. After Print out all family trees

# tuple: 
1.Join two tuples ('a','b','c') and (1,2,3) into aplhanum . Unpack tuple ('a','b','c',1,2,3) into aplha and num. Print out (1,2,3,'a','b','c')
2.Check if the element 7 exists in the tuple (1, 3, 5, 7, 9)
3.Create a tuple (1, 2, 3) and repeat it three times
4.Count the number of occurrences of the element 2 in the tuple (1, 2, 3, 2, 4, 2)
5.Find the index of the first occurrence of the element 3 in the tuple (1, 2, 3, 4, 3, 5)
6.Sort the elements of the tuple (5, 2, 8, 1, 3) and store the result in a new tuple

# list:
1.Create continiousing list from 0 to N, replace from N/2 till N with 'A', and create another list in reverse order of previous with negative search loop. Insert 'p' in the end
